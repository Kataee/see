#include "header.h"
#include "source.c"


int main() {
    /*
    heap *h = createHeap(32);


    FILE *f = fopen("input.txt", "r");
    int buffer;
    while (fscanf(f, "%i", &buffer) != EOF) {
        insert(h, buffer);
    }

    printf("Print as it is:\n");
    print(h);

    printf("\nmax: %i\n", max(h));

    printf("size: %i\n", h->size);
    printf("Print: \n");
    print(h);

    printf("Max: %i\n", torolmax(h));
    print(h);
    fclose(f);
     */
    fel2();

    return 0;
}
